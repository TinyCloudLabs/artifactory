# Short Insights Quality Evaluator

Pass only when every insight has a distinct claim, an explicit implication,
and at least one authorized evidence source. Reject repeated post bodies and
claims whose title or fingerprint is already represented in the supplied prior
artifact metadata. Do not infer prior artifact content from that metadata.
